# cdproxy: CDP proxy for playwright-python

The WebSocket implementation used in playwright-python is broken. This works around that.

Pass the destination URL as a `URL` environment variable. That is all.
